name: Deploy
on:
  push:
    branches:
      - develop    # Auto-deploy to dev
      - staging    # Auto-deploy to staging
      - main       # Auto-deploy to production

jobs:
  deploy-dev:
    if: github.ref == 'refs/heads/develop'
    uses: ninkovski/azure-workflow-actions-v1/.github/workflows/deploy-function.yml@main  # ⚠️ CAMBIAR: ninkovski por tu organización
    with:
      function-app-name: 'func-bartolo-profe-app-process-payment-dev'              # Nombre de tu Azure Function en dev
      resource-group: 'bartolo-profe-rg'                       # Resource Group de dev
      package-path: '.'                                 # Raíz del repositorio (donde está package.json)
      environment: 'dev'
      node-version: '18.x'                              # Cambia si usas otra versión
      run-tests: false
      enable-notifications: false
      create-release-on-success: false

 